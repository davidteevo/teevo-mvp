export function buyerShareMessage(url: string): string {
  return `I'm using Teevo to buy and sell golf clubs. You can get £5 towards your first purchase with my link: ${url}`;
}

export function sellerShareMessage(url: string): string {
  return `Know someone with clubs gathering dust? Invite them to sell on Teevo with my link: ${url}`;
}
